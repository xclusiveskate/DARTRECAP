import 'dart:io';

void main() {
  //STRING METHOD ,STRING METHOD, STRING METHOD
  // String name = "gberugberugbeaaruSDFHVSAfg";
  // String Class = "class2";
  // String level = "level3";
  // print(name.codeUnits);
  // print(name.hashCode);
  // print(name.runes);
  // print(name.codeUnitAt(2));
  // print(name.replaceFirstMapped("l", (match) => "b"));
  // print(name.splitMapJoin(' '));
  // print(name.matchAsPrefix("d"));
  // print(name.replaceAllMapped(name, (match) => "Omooba"));
  // print(name.allMatches(name));
  // print(name.noSuchMethod());
  // print(name.isEmpty);
  // print(name.isNotEmpty);
  // print(name.length);
  // print(name.runtimeType);
  // print(name.compareTo(level)); :come back
  // print(name.contains("S", 20));
  // print(name.endsWith("f"));
  // print(name.indexOf("rug"));
  // print(name.toLowerCase().lastIndexOf("a"));
  // print(name.padLeft(2, "yyy"));
  // print(name.padRight(4, ">>>>"));
  // print(name.replaceAll('a', "w"));
  // print(name.replaceFirst("a", "o"));
  // print(name.replaceRange(3, 10, "ade"));
  // print(name.split(''));
  // print(name.startsWith('g'));
  // print(name.substring(2,4));
  // print(name.toLowerCase());
  // print(name.toUpperCase());
  // print(name.trim);
  // print(name.trimLeft());
  // print(name.trimRight());

  //INTEGER METHOD INTEGER METHOD INTEGER METHOD
  // var a = 253.5760500;
  // print(a.sign);
  // print(a.isFinite);
  // print(a.isInfinite);
  // print(a.isNaN);
  // print(a.isNegative);
  // print(a.runtimeType);
  // print(a.abs());
  // print(a.ceil());
  // print(a.ceilToDouble());
  // print(a.clamp(10, 20));
  // print(a.compareTo(2));
  // print(a.floor());
  // print(a.floorToDouble());
  // print(a.remainder(100));
  // print(a.round());
  // print(a.roundToDouble());
  // print(a.toDouble());
  // print(a.toInt());
  // print(a.toStringAsExponential());
  // print(a.toStringAsFixed(1));
  // print(a.toStringAsPrecision(9));
  // print(a.truncate());
  // print(a.truncateToDouble());

  //List METHOD LIST METHOD LIST METHOD
  var c = ["ade", "shade", "wale", 1, 2];
  var d = [
    {"name": "tolu", "price": 2},
    {"name": "tola", "price": 3}
  ];
  // print(c.addAll());
  // print(c.add(3));
  // print(c.first);
  // print(c.iterator);
  // print(c.cast());
  // print(c.expand((element) => null));
  // print(c.fillRange(1, 3, 2));
  // print(c.fold(initialValue, (previousValue, element) => null));
  // print(c.insert(3, "mine"));
  // print(c.insertAll(5, insertItem));
  // print(c.reduce((value, element) => element == "ade"));
  // print(c.removeRange(2, 3));
  // print(c.setAll(index, iterable));
  // print(c.setRange(start, end, iterable));
  // print(c.skipWhile((c) => c  > 1 ));
  // print(c.toList());
  // print(c.whereType());
  //print(c.toString());

  // print(c.length);
  // print(c.reversed);
  // print(c.runtimeType);
  // print(c.isEmpty);
  // print(c.isNotEmpty);
  // print(c.last);
  // print(d.single);
  // print(c.any((element) => element == "ade"));
  // print(c.asMap());
  // print(c.contains(2));
  // print(c.elementAt(3));
  // print(c.every((element) => element is String || element is int));
  // print(c.firstWhere((element) => element == 2));
  // print(c.followedBy([4,5,6]));
  // print(c.getRange(2, 4));
  // print(c.indexOf("ade"));
  // print(c.indexWhere((element) => element == "ade"));
  // print(c.lastIndexOf("wale"));
  // print(c.lastIndexWhere((c) => c is! int));
  // print(c.lastWhere((element) => element is! int));
  // print(d.map((d) => d["name"]));
  // print(c.remove("wale"));
  // print(c.removeAt(2));
  // print(c.removeLast());
  // print(c.removeWhere((element) => false));
  // print(c.singleWhere((element) => element == "shade"));
  // print(c.skip(2));
  // print(c.sublist(1, c.length));
   // print(c.take(2));
  // print(c.takeWhile((value) => c == 1));
  // print(c.toSet());
  // print(c.where((element) => element < 2));
  // c.clear();
  // c.forEach((element) { });
  // c.replaceRange(2, 4, [""]);
  // c.retainWhere((element) => false);
  // c.shuffle() ;
  // c.sort();



  //a program that picks the median value
  // var list = [2, 3, 4, 5, 3, 45, 67, 88, 555, 545, 78, 150];
  // list.sort();
  // print(list);
  // var median;
  // if (list.length % 2 == 0) {
  //   median = (1 / 2 * ((list.length / 2) + ((list.length / 2) + 1)));
  //   var a = median.floor();
  //   var b = median.ceil();
  //   var c = (list.elementAt(a) + list.elementAt(b)) /2;
  //   // var a =
  //   //     ((list.elementAt(median).ceil() + list.elementAt(median).floor()) / 2);
  //   print(c);
  // } else {
  //   median = ((list.length + 1) / 2).toInt();
  //   var a = list.elementAt(median);
  //   print(a);
  // }
}
